Basic Swift app showcasing basic setup of Zendesk's Product SDKs

### Getting started

1. Fill in the Zendesk credentials in the `AppDelegate`.
2. _(Optional)_ Change the zendesk `themeColor` in `ViewController.swift`.
3. Run the app
